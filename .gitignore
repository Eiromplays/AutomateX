bin/
obj/
.vs/
.idea/
.fleet/
*.user
*.DotSettings.user
.DS_Store
appsettings.*.Local.json
.aspire/
node_modules/
dist/
src/web/build/
src/web/.react-router/
plugins/*/
TestResults/

# Local env / secrets for self-host
.env

# Claude Code per-machine local settings (permissions/overrides) — never shared.
# Keep shared .claude/ config (settings.json, commands, etc.) trackable.
**/.claude/settings.local.json
